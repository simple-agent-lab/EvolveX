# untracked state: survives `git reset` (reset-protected ledger & artifacts)
runs/
archive.jsonl
best_ever.json
insights/
manifests/
ckpts/
.evolve-gen.json
__pycache__/
*.pyc
# uv runtime artifacts — must never dirty a generation diff
.venv/
uv.lock
