# candidate notes

Layer 2 — the thing the Harness scores. At M0 this is a toy genome
(params.json + these notes); from M1 it becomes a real agent definition
runnable by harbor (`candidate:Agent`).

## mutation log
