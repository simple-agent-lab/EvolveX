# MetaAgent Smoke

This is the deterministic offline scaffold for MetaAgent. It preserves the
markdown-focused mutable surface and newest-parent selection while using
`agent_command` meta-agent edits for stubbed tests.
