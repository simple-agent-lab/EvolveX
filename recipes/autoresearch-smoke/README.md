# AutoResearch Smoke

This is the deterministic offline scaffold for AutoResearch. It keeps
`mode: agent` and the single-file mutable surface while routing evaluation
through the Harbor stub and `agent_command` meta-agent.
