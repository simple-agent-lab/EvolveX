# Hill Climb Smoke

This is the deterministic offline scaffold for hill climb. It keeps the Harbor
evaluator contract and `agent_command` meta-agent path so init and loop-shape
tests can run under `EVAL_STUB=1` without cloning MiniSWE.
