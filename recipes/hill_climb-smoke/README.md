# Hill Climb Smoke

This is the deterministic offline scaffold for hill climb. It keeps the Harbor
evaluator contract but uses the fixed meta-agent so init and loop-shape tests can
run under `EVAL_STUB=1` without cloning MiniSWE.
