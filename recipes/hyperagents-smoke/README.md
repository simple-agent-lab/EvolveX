# HyperAgents Smoke

This is the deterministic offline scaffold for HyperAgents. It preserves the
wider mutable surface and randomized parent choice while using the `agent_command` meta-agent
for stubbed tests.
