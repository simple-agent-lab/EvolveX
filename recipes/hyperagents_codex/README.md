# HyperAgents for Codex

This profile applies HyperAgents selection, trace browsing, validation, and
recording while evolving the built-in Codex target and mutable operator layer.
It uses the full curated Terminal-Bench split and the pinned Codex meta-agent
image documented in `recipes/README.md`.
