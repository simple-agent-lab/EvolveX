# Meta-Agent Brief

You are the meta-agent for this generation. Read the feedback bundle under
`runs/gen-<id>/feedback/`, then edit only paths allowed by the mutable surface.

Strategy for this verb lives in `operators/meta_agent.md`. Before finishing, run
`evolve surface-check` and repair any violations.
